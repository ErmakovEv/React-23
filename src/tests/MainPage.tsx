// import { render, screen } from '@testing-library/react';
// import getAllHeadlines from '../components/api/test';

// const mockJsonPromise = Promise.resolve(() => [{ title: 'test1' }, { title: 'test2' }]);
// const mockFetchPromise = Promise.resolve({ json: () => mockJsonPromise });
// global.fetch = jest.fn(() =>
//   Promise.resolve({
//     json: () => Promise.resolve({ articles: 100 }),
//   })
// );

// it('test MainPage loading', async () => {
//   const rate = await getAllHeadlines();
//   expect(rate).toEqual(100);
// });

const foo = () => {};
export default foo;
